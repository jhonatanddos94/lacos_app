'use strict';

/**
 * Job registration hub.
 * Migration / inventory jobs will be wired in later sprints.
 *
 * @param {object} _ParseGlobal
 */
function registerJobs(_ParseGlobal) {
  // Intentionally empty in T1.3.1
}

module.exports = {
  registerJobs,
};
