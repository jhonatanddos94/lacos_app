'use strict';

/**
 * Trigger registration hub.
 * beforeSave / beforeFind modules will be wired in later sprints.
 *
 * @param {object} _ParseGlobal
 */
function registerTriggers(_ParseGlobal) {
  // Intentionally empty in T1.3.1
}

module.exports = {
  registerTriggers,
};
